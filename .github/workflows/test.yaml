on: [push]

jobs:
  hello_world_job:
    runs-on: self-hosted
    name: Build docker containers
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build Action import 
        uses: ./ # Uses an action in the root directory